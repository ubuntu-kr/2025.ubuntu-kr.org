---
import { getRelativeLocaleUrlList } from 'astro:i18n';
import { WebsiteConfig } from 'src/config';

export function getStaticPaths() {
	return getRelativeLocaleUrlList().map((locale) => {
		let localeItem = locale.split('/')[1];
		return {
			params: { locale: localeItem },
		};
	});
}

---
<html lang="ko">
    <head>
        <meta charset="utf-8">
        <meta property="og:title" content={WebsiteConfig.siteTitle}>
        <meta property="og:site_name" content={WebsiteConfig.siteTitle} >
        <meta property="og:url" content="https://2025.ubuntu-kr.org">
        <meta property="og:description" content="2025년 8월 9일, 한국마이크로소프트 13층">
        <meta property="og:type" content="">
        <meta property="og:image" content="https://2025.ubuntu-kr.org/og_default.png">

        <meta name="twitter:card" content="summary_large_image">
        <meta name="twitter:site" content=`@${WebsiteConfig.socialMedia.twitter}`>
        <meta name="twitter:title" content={WebsiteConfig.siteTitle}>
        <meta name="twitter:description" content="2025년 8월 9일, 한국마이크로소프트 13층">
        <meta name="twitter:image" content="https://2025.ubuntu-kr.org/og_default.png">
        <title>{WebsiteConfig.siteTitle}</title>
    </head>
